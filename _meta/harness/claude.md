# Claude Code / Claude Desktop

Open this vault as the project root. `CLAUDE.md` is the contract.

```bash
_meta/alambic session --json --max-tokens 2500 "<question>"
```

Optional MCP stdio: `npm run mcp` with read tools `vault_search`,
`vault_context`, `vault_read`, `vault_health` plus shadow-staging
`vault_capture` / `vault_feedback` (never a `kb/` write).
