# Inbox — staging

Quarantine. Nothing here is durable knowledge until promoted into `kb/` or `ref/`.

| Folder | Who writes |
| --- | --- |
| `manual/` | Human |
| `ai/` | Human after review, or agent drafts |

No secrets, tokens, or raw transcripts. See [[obsidian-hybrid-workflow]].
